# Copy to ~/.config/containers/gerber-to-scad.env and fill in values.
# Generate a key with: python -c "import secrets; print(secrets.token_urlsafe(50))"
SECRET_KEY=change-me
CSRF_TRUSTED_ORIGINS=https://your-domain.example.com
